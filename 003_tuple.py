# tuple
# (), index, Nonmutable(Immutable), duplicates, ordered

my_tuple = (3, 4, 2, 1, 3)

# access first element
print(my_tuple[0])
print(my_tuple[-1])

# change value
# my_tuple[0] = 300

# Ordered